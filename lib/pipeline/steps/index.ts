// Importing a step file registers it with the pipeline registry as a side effect.
// Add new steps by creating a file here and importing it below.
import './transcribe-audio';
import './synthesize-lesson-audio';
import './health-check';
